<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans"
	xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
	xmlns:mybatis-spring="http://mybatis.org/schema/mybatis-spring"
	xmlns:context="http://www.springframework.org/schema/context"
	xsi:schemaLocation="http://mybatis.org/schema/mybatis-spring http://mybatis.org/schema/mybatis-spring-1.2.xsd
		http://www.springframework.org/schema/beans http://www.springframework.org/schema/beans/spring-beans.xsd
		http://www.springframework.org/schema/context http://www.springframework.org/schema/context/spring-context-4.3.xsd">

	<!-- DB정보 있는 파일 불러오기 -->
	<context:property-placeholder location="classpath:config/db.properties" />
	
	<!-- datasource connection pool -->
	<bean id="hikariConfig" class="com.zaxxer.hikari.HikariConfig">
		<property name="driverClassName" value="${driver}" />
		<property name="jdbcUrl" value="${url}" />
		<property name="username" value="${user}" />
		<property name="password" value="${password}" />
	</bean>

	<bean id="dataSource" class="com.zaxxer.hikari.HikariDataSource"
		destroy-method="close">
		<constructor-arg ref="hikariConfig" />
	</bean>


	<!-- mybatis SqlSessionFactory -->
	<bean class="org.mybatis.spring.SqlSessionFactoryBean">
		<property name="dataSource" ref="dataSource" />
		<property name="configLocation" value="classpath:config/mybatis-config.xml"/>

	<!-- 매퍼에 대한 위치를 알려줄 때 사용  -->
  	<!-- xml파일을 정확히 가져올때 사용 -->
  	<!-- 물리적 경로기 때문에 .이아닌 /로 들어가고 기능들이 많을테니 중간 폴더명은 **로 채움 -->
	<property name="mapperLocations" value="classpath*:com.proj.travelog/**/mapper/*Mapper.xml"/>
	</bean>

	<!-- mapper scan -->
	<mybatis-spring:scan base-package="com.proj.travelog.**.mapper" />
	<!-- └> 이 부분은 패키지에 따라 app/spring 등등 달라짐 -->
</beans>
